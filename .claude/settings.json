{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "git add -A 2>/dev/null; git diff --cached --quiet || git commit -m \"auto: $(date '+%Y-%m-%d %H:%M')\" 2>/dev/null; git push origin main 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
